---
phase: 04-ai-wizard-substeps
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - data/test-cases/catalog.json
  - data/test-cases/product.json
  - data/test-cases/cart.json
  - data/test-cases/checkout.json
  - data/test-cases/auth.json
  - data/test-cases/search.json
  - data/test-cases/blog.json
  - data/test-cases/contact-form.json
  - data/test-cases/multilang.json
  - data/test-cases/coupons.json
  - src/components/wizard/ResultsView.tsx
autonomous: true
requirements:
  - GEN-05
  - GEN-06

must_haves:
  truths:
    - "Every test case entry in every JSON file has a 'Feature' field whose value matches exactly one of the strings in MODULE_FEATURES for that module"
    - "filterCasesForModule accepts moduleFeatures as a parameter and applies feature filtering when a module has checked features"
    - "Entries without 'Feature' are always included regardless of what features are checked"
    - "0 features checked for a module → include all entries for that module (backwards compatible)"
    - "filterCasesForModule still applies the existing ID-based exclusion logic (checkout/search/contact-form/multilang detail filters remain)"
  artifacts:
    - path: "data/test-cases/catalog.json"
      provides: "Annotated catalog test cases with Feature field"
      contains: "Feature"
    - path: "data/test-cases/blog.json"
      provides: "Annotated blog test cases with Feature field"
      contains: "Feature"
    - path: "src/components/wizard/ResultsView.tsx"
      provides: "Updated filterCasesForModule accepting moduleFeatures parameter"
      contains: "moduleFeatures"
  key_links:
    - from: "src/components/wizard/ResultsView.tsx"
      to: "data/test-cases/*.json"
      via: "filterCasesForModule reads tc.Feature against state.moduleFeatures[moduleId]"
      pattern: "tc\\.Feature"
---

<objective>
Annotate all 10 test case JSON files with a "Feature" field on each entry, mapping each test case to the specific feature it covers. Update filterCasesForModule() in ResultsView.tsx to accept moduleFeatures and filter accordingly.

Purpose: Delivers feature-based filtering — when a user checks specific features in the sub-steps, only test cases covering those features (plus always-include entries without Feature) appear in results.

Output:
- All 10 JSON files have "Feature" field on every entry
- filterCasesForModule() updated to accept and apply moduleFeatures
- Fallback: no Feature field → always include; 0 features checked → include all
</objective>

<execution_context>
@C:/Users/maksi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maksi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ai-wizard-substeps/04-CONTEXT.md
@.planning/phases/04-ai-wizard-substeps/04-TASK-SPEC.md
@.planning/phases/04-ai-wizard-substeps/04-01-SUMMARY.md
</context>

<interfaces>
<!-- Contracts from Plan 01 that this plan builds upon. -->

From src/lib/types.ts (after Plan 01):
```typescript
export type TestCase = {
  ID: string;
  Назва: string;
  Suite: string;
  Передумови: string;
  Кроки: string[];
  "Очікуваний результат": string;
  Type: "E2E" | "Manual" | "Unit" | "Component" | "Integration";
  Layer: "Smoke" | "Regression" | "Feature";
  Пріоритет: "P0" | "P1" | "P2";
  Feature?: string;   // Added in Plan 01
};

export type WizardState = {
  // ... existing fields ...
  moduleFeatures: Record<string, string[]>;  // Added in Plan 01
};
```

From src/constants/wizard-config.ts (after Plan 01):
```typescript
export const MODULE_FEATURES: Record<string, string[]> = {
  catalog: ["список товарів із пагінацією", "фільтрація за категорією", "сортування (ціна/назва/новизна)", "пошук у каталозі", "картка товару в списку (фото/ціна/назва)", "значок «Немає в наявності»", "швидкий перегляд товару", "порівняння товарів", "нескінченний скрол / кнопка «Завантажити ще»", "теги та мітки (новинка/акція/хіт)"],
  product: ["назва/ціна/опис/фото", "галерея зображень", "вибір варіанту (розмір/колір)", "кількість товару", "кнопка «Додати до кошика»", "кнопка «Купити зараз»", "блок «Схожі товари»", "відгуки та рейтинг", "вкладки (опис/характеристики/відгуки)", "відеоогляд товару", "наявність на складі", "SEO-метадані"],
  cart: ["перегляд вмісту кошика", "зміна кількості", "видалення товару", "збереження кошика після перезавантаження", "міні-кошик у шапці", "порожній кошик (стан)", "перехід до оформлення"],
  checkout: ["форма доставки", "форма оплати", "вибір способу доставки", "вибір способу оплати", "застосування промокоду", "підсумок замовлення", "гостьове оформлення", "підтвердження замовлення (email)", "відмова платежу"],
  auth: ["реєстрація", "вхід", "вихід", "відновлення пароля", "валідація полів", "вхід через соцмережі", "особистий кабінет", "історія замовлень", "редагування профілю"],
  search: ["пошук за точною назвою", "результат «нічого не знайдено»", "автодоповнення", "пошук з фільтрами", "пошук одним символом", "пошук з максимальною довжиною запиту"],
  blog: ["список статей із пагінацією", "сторінка окремої статті", "категорії/рубрики", "теги", "пошук по блогу", "фільтрація/сортування статей", "коментарі", "поділитися в соцмережах", "схожі/рекомендовані статті", "RSS-стрічка", "автор статті", "дата публікації", "прев'ю/обкладинка статті"],
  "contact-form": ["відправка форми", "валідація email", "прикріплення файлу", "ліміт розміру файлу", "CAPTCHA", "пробіли в обов'язкових полях"],
  multilang: ["перемикання мови", "збереження між сторінками", "відображення трьох мов", "коректність символів", "збереження після перезавантаження"],
  coupons: ["застосування валідного купону", "прострочений купон", "мінімальна сума замовлення", "два купони одночасно", "пробіли в коді купону"],
};
```

Current filterCasesForModule signature (to be updated):
```typescript
function filterCasesForModule(
  moduleId: string,
  state: WizardState,
  allCases: TestCase[]
): TestCase[]
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Annotate all 10 JSON files with "Feature" field</name>
  <read_first>
    - data/test-cases/catalog.json — read before editing; 6 entries
    - data/test-cases/product.json — read before editing
    - data/test-cases/cart.json — read before editing
    - data/test-cases/checkout.json — read before editing; 8 entries
    - data/test-cases/auth.json — read before editing; 7 entries
    - data/test-cases/search.json — read before editing
    - data/test-cases/blog.json — read before editing; 5 entries
    - data/test-cases/contact-form.json — read before editing
    - data/test-cases/multilang.json — read before editing
    - data/test-cases/coupons.json — read before editing
    - src/constants/wizard-config.ts — reference MODULE_FEATURES strings (exact values to use)
  </read_first>
  <files>
    data/test-cases/catalog.json,
    data/test-cases/product.json,
    data/test-cases/cart.json,
    data/test-cases/checkout.json,
    data/test-cases/auth.json,
    data/test-cases/search.json,
    data/test-cases/blog.json,
    data/test-cases/contact-form.json,
    data/test-cases/multilang.json,
    data/test-cases/coupons.json
  </files>
  <action>
Add a "Feature" field to every entry in each JSON file. The "Feature" value MUST be one of the strings in MODULE_FEATURES for that module. The value must match exactly (case-sensitive, character-exact) — no paraphrasing.

Place "Feature" after "Suite" and before "Передумови" in each entry object.

Assignment guidance — map each test case to the most specific feature it primarily tests:

catalog.json (MODULE_FEATURES key: "catalog"):
- TC-CAT-001 → "картка товару в списку (фото/ціна/назва)"
- TC-CAT-002 → "фільтрація за категорією"
- TC-CAT-003 → "сортування (ціна/назва/новизна)"
- TC-CAT-004 → "список товарів із пагінацією"
- TC-CAT-005 → "значок «Немає в наявності»"
- TC-CAT-006 → "картка товару в списку (фото/ціна/назва)"

product.json (MODULE_FEATURES key: "product"):
- TC-PDP-001 → "назва/ціна/опис/фото"
- TC-PDP-002 → "галерея зображень"
- TC-PDP-003 → "вибір варіанту (розмір/колір)"
- TC-PDP-004 → "кнопка «Додати до кошика»"
- TC-PDP-005 → "відгуки та рейтинг"

cart.json (MODULE_FEATURES key: "cart"):
- TC-CART-001 → "перегляд вмісту кошика"
- TC-CART-002 → "зміна кількості"
- TC-CART-003 → "видалення товару"
- TC-CART-004 → "порожній кошик (стан)"

checkout.json (MODULE_FEATURES key: "checkout"):
- TC-CHK-001 → "форма доставки"
- TC-CHK-002 → "форма доставки"
- TC-CHK-003 → "гостьове оформлення"
- TC-CHK-004 → "відмова платежу"
- TC-CHK-005 → "застосування промокоду"
- TC-CHK-006 → "підсумок замовлення"
- TC-CHK-007 → "форма доставки"
- TC-CHK-008 → "форма доставки"

auth.json (MODULE_FEATURES key: "auth"):
- TC-AUTH-001 → "реєстрація"
- TC-AUTH-002 → "вхід"
- TC-AUTH-003 → "вхід"
- TC-AUTH-004 → "відновлення пароля"
- TC-AUTH-005 → "вихід"
- TC-AUTH-006 → "реєстрація"
- TC-AUTH-007 → "валідація полів"

search.json (MODULE_FEATURES key: "search"):
- TC-SRCH-001 → "пошук за точною назвою"
- TC-SRCH-002 → "результат «нічого не знайдено»"
- TC-SRCH-003 → "автодоповнення"
- TC-SRCH-004 → "пошук з фільтрами"

blog.json (MODULE_FEATURES key: "blog"):
- TC-BLOG-001 → "список статей із пагінацією"
- TC-BLOG-002 → "сторінка окремої статті"
- TC-BLOG-003 → "категорії/рубрики"
- TC-BLOG-004 → "дата публікації"
- TC-BLOG-005 → "прев'ю/обкладинка статті"

contact-form.json (MODULE_FEATURES key: "contact-form"):
- TC-FORM-001 → "відправка форми"
- TC-FORM-002 → "валідація email"
- TC-FORM-003 → "прикріплення файлу"
- TC-FORM-004 → "CAPTCHA"

multilang.json (MODULE_FEATURES key: "multilang"):
- TC-LANG-001 → "перемикання мови"
- TC-LANG-002 → "збереження між сторінками"
- TC-LANG-003 → "відображення трьох мов"
- TC-LANG-004 → "збереження після перезавантаження"

coupons.json (MODULE_FEATURES key: "coupons"):
Read the existing entries and assign each to the most specific matching feature:
- Entries about valid coupon application → "застосування валідного купону"
- Entries about expired coupon → "прострочений купон"
- Entries about minimum order amount → "мінімальна сума замовлення"
- Entries about two coupons simultaneously → "два купони одночасно"
- Entries about coupon code with spaces → "пробіли в коді купону"

IMPORTANT: The "Feature" string value must be byte-identical to the corresponding string in MODULE_FEATURES. Check each value against the wizard-config.ts MODULE_FEATURES definition after Plan 01 runs.
  </action>
  <verify>
    <automated>cd /c/folder1/qa_app && node -e "const fs=require('fs');const files=['catalog','product','cart','checkout','auth','search','blog','contact-form','multilang','coupons'];let ok=true;for(const f of files){const arr=JSON.parse(fs.readFileSync('data/test-cases/'+f+'.json','utf8'));for(const tc of arr){if(!tc.Feature){console.error('MISSING Feature in '+f+' ID:'+tc.ID);ok=false;}}}if(ok)console.log('ALL OK - every entry has Feature field');"</automated>
  </verify>
  <done>
    - All 10 JSON files have "Feature" field on every entry
    - Node verification script prints "ALL OK - every entry has Feature field"
    - Each Feature value is one of the exact MODULE_FEATURES strings for that module
    - JSON files remain valid (parse without errors)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update filterCasesForModule to accept and apply moduleFeatures</name>
  <read_first>
    - src/components/wizard/ResultsView.tsx — read full file; focus on filterCasesForModule and its two call sites
    - .planning/phases/04-ai-wizard-substeps/04-CONTEXT.md — decisions D-12, D-13, D-14
  </read_first>
  <files>src/components/wizard/ResultsView.tsx</files>
  <action>
Update `filterCasesForModule` in ResultsView.tsx (per D-12, D-13):

Change the function signature to add `moduleFeatures: Record<string, string[]>` as a third parameter (before `allCases`):
```
function filterCasesForModule(
  moduleId: string,
  state: WizardState,
  moduleFeatures: Record<string, string[]>,
  allCases: TestCase[]
): TestCase[]
```

After the existing ID-based exclusion logic (keep all existing checkout/search/contact-form/multilang exclusions exactly as they are — per D-14), add feature filtering as the final step:

```typescript
// Feature filtering (Phase 4, per D-12)
const selectedFeatures = moduleFeatures[moduleId] ?? [];
if (selectedFeatures.length > 0) {
  cases = cases.filter(
    (tc) => !tc.Feature || selectedFeatures.includes(tc.Feature)
  );
}
```

Rules encoded by this logic:
- `!tc.Feature` → entry has no Feature field → always include (backwards compatible per D-14)
- `selectedFeatures.includes(tc.Feature)` → Feature matches one of the checked features → include
- If `selectedFeatures.length === 0` → skip filter entirely → include all entries (per D-12 fallback)

Update the two call sites of filterCasesForModule in the component body. Both currently pass `(moduleId, state, allCases)`. Change both to `(moduleId, state, state.moduleFeatures, allCases)`:

1. In `filteredModules` derivation: `filterCasesForModule(m, state, state.moduleFeatures, allCases).length > 0`
2. In `casesByModule` useMemo: `filterCasesForModule(m, state, state.moduleFeatures, allCases)`

Do not change anything else in ResultsView.tsx in this plan — the table rendering, buildMarkdown, and buildCsv are updated in Plan 03.
  </action>
  <verify>
    <automated>cd /c/folder1/qa_app && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - filterCasesForModule signature has 4 parameters: moduleId, state, moduleFeatures, allCases
    - Feature filter logic: `!tc.Feature || selectedFeatures.includes(tc.Feature)` applied when selectedFeatures.length > 0
    - Both call sites updated to pass state.moduleFeatures as third argument
    - All existing ID-based exclusion logic (checkout, search, contact-form, multilang) preserved unchanged
    - TypeScript 0 errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON files → filterCasesForModule | Feature strings from JSON are compared against user-selected feature strings from WizardState |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | data/test-cases/*.json | accept | JSON files are static build artifacts; no user input writes to them; read-only at runtime |
| T-04-04 | Information Disclosure | Feature field in JSON | accept | Feature strings are non-sensitive QA metadata; shipped in bundle |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After both tasks complete:
1. Run `npx tsc --noEmit` — 0 errors
2. Node verification script outputs "ALL OK - every entry has Feature field"
3. Run `npm run dev`; complete wizard selecting "Блог" with only "список статей із пагінацією" checked → results table shows TC-BLOG-001 only (other blog entries filtered out)
4. Same wizard with no features checked for Блог → all 5 blog entries appear
5. Existing detail-flag exclusions still work: checkout without promo code → TC-CHK-005 not included
</verification>

<success_criteria>
- All 10 JSON files: every entry has "Feature" field matching a MODULE_FEATURES string for that module
- filterCasesForModule: 4-parameter signature; feature filter applied after existing exclusion logic
- Feature filter: entries without Feature always pass; 0 selected features → all pass; N selected features → only entries whose Feature is in the selected set pass
- TypeScript 0 errors; app runs without runtime errors
</success_criteria>

<output>
Create `.planning/phases/04-ai-wizard-substeps/04-02-SUMMARY.md` when done.
</output>
